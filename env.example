# Example Environment Configuration
# Copy this file to .env and update with your values

# Environment: development or production
FLASK_ENV=development

# Enable MySQL (set to false to use SQLite)
USE_MYSQL=true

# Remote MySQL Database Configuration
DB_HOST=192.168.11.1
DB_PORT=3306
DB_USER=remote_user
DB_PASS=BuGr@d@N4@loB6!

# Database names for different environments
DB_NAME_DEV=nutri_tracker_dev
DB_NAME_PROD=nutri_tracker_prod

# Docker Network Bridge Configuration
# Use this if running in Docker and need to access host MySQL
# Options: host.docker.internal, 172.17.0.1, or actual host IP
DOCKER_DB_HOST=host.docker.internal

# SQLite Configuration (for migration source)
LOCAL_SQLITE_PATH=database.db
PROD_SQLITE_PATH=/root/nutrition_tracker_data/database.db

# Application Settings
SECRET_KEY=change-this-to-a-secure-random-string
DEBUG=true
SESSION_TIMEOUT=30

# OpenAI Configuration (if using AI features)
OPENAI_API_KEY=your-openai-api-key-here
